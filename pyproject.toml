[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "macro-dislocation-lab"
version = "0.9.0"
description = "Point-in-time macro release ingestion and post-announcement dislocation research"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Macro Dislocation Lab contributors"}]
dependencies = []

[project.scripts]
macro-lab = "macro_dislocation.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
